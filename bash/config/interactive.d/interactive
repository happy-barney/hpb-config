
eval `dircolors ~/.local/share/bash/dircolors`
stty erase ^H

export SHELL HOME
export ignoreeof=5
export PAGER=less
export HOSTFILE=
export LS_OPTIONS='--color=tty -F -N'

# Local Variables:
# mode: sh
# End:
