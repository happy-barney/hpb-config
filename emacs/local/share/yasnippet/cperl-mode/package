# -*- mode: snippet; require-final-newline: nil -*-
# name: package ... [version]
# key: package
# binding: C-c + i p
# expand-env: ((yas-indent-line 'fixed) (yas-wrap-around-region 'nil))
# --
`""`
use v5.${1:14};
use warnings;

${2:package ${3:`(hpb-perl-guess-package-name)`} v${4:1.0.0} {
	$0

	1;
};
}
